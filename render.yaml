services:
  - type: web
    name: personality-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
